use tauri::{AppHandle, WebviewUrl, WebviewWindowBuilder, webview::DownloadEvent};
// We just want to check if the docs say what the bool does. Let's run a python script to parse the docs for it, or just use `cargo doc`.
